'use stricts';

/**

 */

var LEDTest = {
  init: function ledt_init() {

  },
  uninit: function ledt_uninit() {

  }
};

window.addEventListener('load', LEDTest.init.bind(LEDTest));
window.addEventListener('unload', LEDTest.uninit.bind(LEDTest));
